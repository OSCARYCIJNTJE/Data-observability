  dbt-pipeline:
    build:
      context: .
      dockerfile: dockerfile.dbt
    depends_on:
      - kafka
      - ksqldb
      - construction-app
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092